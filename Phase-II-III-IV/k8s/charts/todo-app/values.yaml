# Generated by kubectl-ai (Simulated)
global:
  env: "local"

backend:
  replicaCount: 2
  image:
    repository: todo-backend
    tag: latest
    pullPolicy: Never # Critical for Minikube local images
  service:
    type: ClusterIP
    port: 8000
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 128Mi

frontend:
  replicaCount: 1
  image:
    repository: todo-frontend
    tag: latest
    pullPolicy: Never
  service:
    type: ClusterIP
    port: 3000
  ingress:
    enabled: true
    host: todo.local
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 128Mi
  env:
    # Secrets are now loaded from k8s secret 'todo-secrets'
    BETTER_AUTH_URL: "http://todo.local" # Required by better-auth server-side
    NEXT_PUBLIC_BETTER_AUTH_URL: "http://todo.local"
    NEXT_PUBLIC_API_URL: "http://todo.local/api" # Use Ingress path for client-side calls

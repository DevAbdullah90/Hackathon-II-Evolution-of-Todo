# Claude Code Instructions

- This project uses `uv` for dependency management.
- Source code is in `src/`.
- Tests are in `tests/`.
- Run tests with `uv run pytest`.
- Run CLI with `uv run todo ...` (once configured in pyproject.toml)
